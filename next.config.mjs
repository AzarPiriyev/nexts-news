/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos'], // Buraya kullanmak istediğiniz alan adını ekleyin
    },
};

export default nextConfig; // Eğer mjs dosyasıysa

